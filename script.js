/* script.js — DEPRECATED
   Este arquivo foi mantido por compatibilidade, mas a lógica da galeria
   foi consolidada em `gallery.js` e as páginas agora carregam `gallery.js`
   diretamente. Remova este arquivo se tiver certeza de que nenhuma página
   externa o referencia.
*/

// wrapper de compatibilidade (não faz nada)
/* global initGallery */
(function(){
    // noop
})();
